# docker-compose.yml
version: "3"
services:
  database:
    image: "postgres"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=boysr10px3
      - POSTGRES_DB=glee
    ports:
      - "5532:5432"
